There are four different folder which i use to practice react 
1. card-project
2. Employee Management System (with bootstrap and custom css)
3. UI of a Web-page using react (made with react and Tailwind CSS)
4. User App (getting or searching user from list of users and also using react routing to link the pages included  REST API's);
5. React-Hooks ( There are 2 folder in src inwhich one of them is "UseStateHook" and second is "UseStateInput")
   UseState-Hook => I Practices with small counter function in this i increses and decreases counter value by useState hook;
   UseStateInput => in this same i used React useState Hook to track the input values .
